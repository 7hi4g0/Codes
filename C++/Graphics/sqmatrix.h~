class SqMatrix : public Matrix{
public:
	SqMatrix(unsigned int side):Matrix(side);
	
	void identity();
	void translate(float x, float y, float z);
	void scale(float x, float y, float z);
	void rotate(float angle, float x, float y, float z);
}
